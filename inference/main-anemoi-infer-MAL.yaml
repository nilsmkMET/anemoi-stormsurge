# Docs:
# https://anemoi.readthedocs.io/projects/inference/en/latest/inference/configs/top-level.html

checkpoint: /lustre/storeB/users/nilsmk/anemoi-datasets/output/longrun_graph10.3_1980-2012/checkpoint/80d41f7a364d4665813503df9bb0a963/inference-anemoi-by_step-epoch_000-step_STEPHERE.ckpt

date: DATEHERE #specifies the reference date of the forecast (the starting date)

lead_time: 240h # specifies the forecast lead time in hours
# lead_time: 8736h # specifies the forecast lead time in hours

#device: cuda
# runner: parallel # run across multiple gpus
# runner: cutout

input: test # use the dataset that was used during training as input. test (default), training or validation

# You can also provide a full dataset specification as follows:
# (e.g. as done in training)
#input:
#  dataset:
#    cutout:
#      - dataset: ...

#output:
#  netcdf: /pfs/lustrep3/scratch/project_465001902/experiments/lam-gridind-res10-2024-cloud/inference/full-reset.nc

# Or get only output on the lam area
output:
  extract_lam:
    output:
      netcdf:
        path: /lustre/storeB/users/nilsmk/anemoi-datasets/output/longrun_graph10.3_1980-2012/inference/DATEHERE_EXPNAME_stepSTEPHERE.nc

# its also possible to output truth
